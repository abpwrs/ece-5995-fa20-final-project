function query_two_state_update(state_name) {
    window.location.pathname = "/query2/" + state_name;
}

function query_three_state_update(state_name) {
    window.location.pathname = "/query3/" + state_name;
}

function query_four_state_update(state_name) {
    window.location.pathname = "/query4/" + state_name;
}